> Нужно завести переменные окружения (например FEEDBACK_NAME_MAX_LENGTH, FEEDBACK_COMMENT_MAX_LENGTH)

Несомненно!
а также внести во все спеки, где упоминаются env-переменные. В том числе в файл .env.example.

Кстати!

все наши env-переменные в файл .env.example
нужно вписывать в самый верх, то есть перед `APP_NAME=Laravel`


> валидация max: сработает на сыром значении (с пробелами/мусором), а не на очищенном — то есть строка ровно в 2000 «полезных» символов + пробелы по краям может не пройти валидацию

это осознанно (нужно это добавить в тех.спеку)


> MetricsController → 425 Too Early — вот тут, кажется, реальная проблема:

конечно, 501 Not Implemented

поправь §4 и TESTING.md под него

> «стандартные возможности Laravel по санитизации, все доступные для строк» — тут аккуратнее

как тебе такое?

```
use Illuminate\Support\Str;

$clean = collect($request->all())->map(function ($value) {
    if (!is_string($value)) return $value;
    $sanitized = Str::of($value)->trim()->stripTags()->squish();
    return preg_replace('/[\x00-\x1F\x7F]/u', '', (string) $sanitized);
})->all();
```
